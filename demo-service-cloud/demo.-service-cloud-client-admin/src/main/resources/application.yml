# 基础配置
server:
  port: 8083
# 服务配置
spring:
  application:
    name: client-admin
# 注册中心
eureka:
  client:
    service-url:
      defaultZone: http://localhost:11111/eureka
# 监控断点
management:
  endpoints:
    web: # HTTP端点配置
      exposure:
        include: '*'
        exclude: